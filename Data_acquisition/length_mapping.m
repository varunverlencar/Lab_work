%%*************************************************************************
%   length = length_mapping(out);
%   Maps the received length
% 
% 
% 
% 
% *************************************************************************

function length = length_mapping(out)
%TODO : get an accurate mapping function
    length = mean(out);
end